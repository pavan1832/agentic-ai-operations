# agentic-ai-operations
Multi-agent AI assistant that converts natural language tasks into structured plans, executes real APIs (GitHub, Weather, News, Exchange Rates), and returns verified results using LLM-powered reasoning. Built with Planner-Executor-Verifier agent architecture.
